# CS2 Entity List 逆向工程完整教程

## 概述

本教程将从零开始教你如何逆向工程 CS2 的 Entity List 结构，找到正确的内存偏移和 stride 值。适合制作视频教程和教学使用。

**目标受众**: 具有 C/C++ 基础和内存概念的程序员

**你将学到**:
- 如何查找和分析游戏实体结构
- 使用 Cheat Engine 进行内存分析
- 理解基于 Handle 的实体系统
- 计算内存 stride 和偏移量
- 通过编程验证你的发现

**所需时间**: 首次学习需要 2-3 小时

---

## 目录

1. [前置知识](#前置知识)
2. [核心概念](#核心概念)
3. [工具准备](#工具准备)
4. [分步教程](#分步教程)
5. [内存结构图解](#内存结构图解)
6. [验证方法](#验证方法)
7. [常见陷阱](#常见陷阱)
8. [进阶话题](#进阶话题)

---

## 前置知识

### 必备知识点

开始之前，你需要理解以下概念：

#### 1. **内存基础**
```
虚拟内存地址: 0x7FF6A2B40000
             └─ 指向进程内存中某个位置的 64 位地址

指针: 存储内存地址的变量
示例: uintptr_t entityPtr = 0x36bc8048000;
```

#### 2. **十六进制和二进制**
```
十进制: 1052
十六进制: 0x41C
二进制:   0000 0100 0001 1100

为什么用十六进制？更容易阅读内存地址和偏移量
```

#### 3. **位运算**
```cpp
& (按位与)  - 提取位:      0x41C & 0x7FFF = 0x41C
>> (右移)   - 除以 2^n:    1052 >> 9 = 2 (除以 512)
<< (左移)   - 乘以 2^n:    2 << 9 = 1024 (乘以 512)
```

#### 4. **指针解引用**
```cpp
uintptr_t address = 0x1000;
uintptr_t value = *(uintptr_t*)address;  // 读取地址 0x1000 处的 8 字节

// 在 Cheat Engine 中: [address] 表示"读取地址处的值"
```

#### 5. **数据结构**
```cpp
// 指针数组
uintptr_t chunks[8] = {
    0x36c1e8f0000,  // chunk[0]
    0x36c1e8f1000,  // chunk[1]
    0x36c1e8f2000 ,  // chunk[2]
    // ...
};

// 访问: chunks[2] 得到 0x36c1e8f2000
```

### 必备工具

| 工具 | 用途 | 下载地址 |
|------|------|----------|
| **Cheat Engine 7.5+** | 内存扫描和调试器 | [cheatengine.org](https://www.cheatengine.org/) |
| **CS2** | 目标游戏 | Steam |
| **Visual Studio 2022** | 编写验证代码 | [visualstudio.com](https://visualstudio.microsoft.com/) |
| **a2x cs2-dumper** | 获取初始偏移量 | [GitHub](https://github.com/a2x/cs2-dumper) |
| **HxD (可选)** | 十六进制编辑器 | [mh-nexus.de](https://mh-nexus.de/en/hxd/) |

### 推荐技能

- 基础 C/C++ 编程能力
- 理解游戏循环和实体概念
- 熟悉 Windows 进程内存
- 耐心和细心

---

## 核心概念

### 什么是 Entity List？

**Entity List（实体列表）** 是游戏引擎中用于管理所有游戏对象的核心数据结构。

```
┌─────────────────────────────────────────────────────────┐
│                    游戏世界                              │
├─────────────────────────────────────────────────────────┤
│  玩家 1 ──┐                                             │
│  玩家 2 ──┤                                             │
│  玩家 3 ──┤                                             │
│  武器 1 ──┼──> 全部存储在 Entity List 中                │
│  武器 2 ──┤                                             │
│  手雷 1 ──┤                                             │
│  C4炸弹 ──┘                                             │
└─────────────────────────────────────────────────────────┘
```

**为什么需要找到 Entity List？**
- ESP（透视）需要遍历所有玩家
- 自瞄需要找到敌人位置
- 雷达 Hack 需要显示所有实体

### CS2 的双实体系统

CS2 使用了一个独特的双实体系统：**Controller** 和 **Pawn**

```
┌──────────────────────┐         ┌──────────────────────┐
│   Controller         │         │      Pawn            │
│   (控制器)           │         │      (实体)          │
├──────────────────────┤         ├──────────────────────┤
│ • 玩家名称           │         │ • 3D 坐标 (x,y,z)    │
│ • 队伍 (T/CT)        │         │ • 血量 (Health)      │
│ • 存活状态           │         │ • 护甲 (Armor)       │
│ • 金钱               │  m_hPawn│ • 模型和动画         │
│ • m_hPawn ──────────────────>  │ • 碰撞盒             │
│   (Handle)           │         │ • 速度向量           │
└──────────────────────┘         └──────────────────────┘
     索引: 1-64                       索引: 动态分配
```

**关键理解**：
- **Controller**: 存储玩家的"逻辑"信息（谁在玩，哪个队伍）
- **Pawn**: 存储玩家的"物理"信息（在哪里，多少血）
- **m_hPawn**: 一个 Handle（句柄），用于从 Controller 找到对应的 Pawn

**为什么要分开？**
```
场景 1: 玩家死亡
  Controller 保持不变 (玩家还在服务器)
  Pawn 被销毁 (尸体消失)
  m_hPawn = 0 (无效)

场景 2: 玩家重生
  Controller 保持不变
  创建新的 Pawn (新位置)
  m_hPawn 指向新 Pawn
```

---

## 完整的内存变换关系图

### 真实案例：从 Controller 到 Pawn 的完整过程

以下是一个真实的玩家数据示例，展示完整的变换过程：

```
═══════════════════════════════════════════════════════════════════════════
                    从 Controller 到 Pawn 的完整旅程
═══════════════════════════════════════════════════════════════════════════

第一步：读取 Controller
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

假设我们要读取玩家索引 1 的信息：

┌─────────────────────────────────────────────────────────────────────┐
│  client.dll + dwEntityList                                          │
│  读取这个地址得到 Entity List 指针                                   │
└────────────────────────┬────────────────────────────────────────────┘
                         │
                         ▼
              ┌──────────────────────┐
              │  Entity List 基址    │
              │  例如: 0x332089C0000 │
              └──────────┬───────────┘
                         │
                         │ 计算 Controller 地址
                         │ playerIndex = 1
                         │ chunkIndex = 1 >> 9 = 0 (第 1 个 Chunk)
                         │ indexInChunk = 1 & 0x1FF = 1
                         │
                         ▼
              ┌──────────────────────────────────────────┐
              │ 步骤 1: 读取 Chunk[0] 指针               │
              │ 地址 = EntityList + 0x10 + 0x8 * 0      │
              │      = 0x332089C0000 + 0x10             │
              │      = 0x332089C0010                    │
              │                                          │
              │ 读取值 = 0x33208CF0008 ← Chunk[0] 指针  │
              └──────────────────┬───────────────────────┘
                                 │
                                 ▼
              ┌──────────────────────────────────────────┐
              │ 步骤 2: 读取 Controller 指针             │
              │ 地址 = Chunk[0] + 0x70 * 1              │
              │      = 0x33208CF0008 + 0x70             │
              │      = 0x33208CF0078                    │
              │                                          │
              │ 读取值 = 0x332CFD50000 ← Controller 地址│
              └──────────────────┬───────────────────────┘
                                 │
                                 ▼
┌────────────────────────────────────────────────────────────────────────┐
│                    Controller 对象 (0x332CFD50000)                     │
├────────────────────────────────────────────────────────────────────────┤
│  +0x000: VTable 指针                                                   │
│  +0x010: 基类数据                                                      │
│  ...                                                                   │
│  +0x62C: m_hPawn = 0x005E80C0  ◄─── 这是 Handle，不是地址！          │
│  +0x640: m_iszPlayerName = "Player1"                                  │
│  +0x6F0: m_iPing = 25                                                 │
│  ...                                                                   │
└────────────────────────────────────────────────────────────────────────┘


第二步：解析 Handle
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

从 Controller 读取到的 m_hPawn = 0x005E80C0

┌─────────────────────────────────────────────────────────────────────┐
│  Handle 结构分析                                                     │
├─────────────────────────────────────────────────────────────────────┤
│                                                                      │
│  Handle (32位): 0x005E80C0                                          │
│                                                                      │
│  二进制表示:                                                         │
│  0000 0000 0101 1110 1000 0000 1100 0000                            │
│  │                   └──────┬──────┘                                │
│  │                          │                                       │
│  │                    低 15 位 = Entity Index                       │
│  │                                                                  │
│  └─ 高位包含序列号和标志位                                          │
│                                                                      │
├─────────────────────────────────────────────────────────────────────┤
│  提取 Index (取低 15 位):                                            │
│                                                                      │
│  index = handle & 0x7FFF                                            │
│        = 0x005E80C0 & 0x00007FFF                                    │
│        = 0x000000C0                                                 │
│        = 192 (十进制)                                               │
│                                                                      │
├─────────────────────────────────────────────────────────────────────┤
│  计算 Chunk Index (除以 512):                                        │
│                                                                      │
│  chunkIndex = index >> 9                                            │
│             = 192 >> 9                                              │
│             = 0                                                     │
│                                                                      │
│  解释: 192 / 512 = 0 (整数除法)                                     │
│        所以 Pawn 在第 1 个 Chunk (Chunk[0])                         │
│                                                                      │
├─────────────────────────────────────────────────────────────────────┤
│  计算 Chunk 内偏移 (对 512 取模):                                    │
│                                                                      │
│  indexInChunk = index & 0x1FF                                       │
│               = 192 & 0x1FF                                         │
│               = 192                                                 │
│                                                                      │
│  解释: 192 % 512 = 192                                              │
│        所以 Pawn 是 Chunk 内的第 193 个实体 (从 0 开始)             │
│                                                                      │
└─────────────────────────────────────────────────────────────────────┘

总结:
  pawnHandle = 0x5E80C0
  pawnIndex = 192
  pawnChunk = 0
  pawnInChunk = 192


第三步：定位 Pawn 在 Entity List 中的位置
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

现在我们知道 Pawn 在 Chunk[0] 的第 192 个位置，开始读取：

┌─────────────────────────────────────────────────────────────────────┐
│  Entity List 基址: 0x332089C0000                                    │
└────────────────────────┬────────────────────────────────────────────┘
                         │
                         │ 我们需要 Chunk[0]
                         │
                         ▼
              ┌──────────────────────────────────────────┐
              │ 步骤 1: 读取 Chunk[0] 指针               │
              │                                          │
              │ 地址 = EntityList + 0x10 + 0x8 * chunkIndex │
              │      = 0x332089C0000 + 0x10 + 0x8 * 0   │
              │      = 0x332089C0010                    │
              │                                          │
              │ 读取值 = 0x33208CF0008 ◄── pawnListEntry│
              └──────────────────┬───────────────────────┘
                                 │
                                 │ 这是 Chunk[0] 的起始地址
                                 │
                                 ▼
┌────────────────────────────────────────────────────────────────────────┐
│                   Chunk[0] 内存布局 (0x33208CF0008)                    │
├────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  +0x000 (Entity[0]):   0x0000000000000000  (空)                        │
│  +0x070 (Entity[1]):   0x332CFD51000                                   │
│  +0x0E0 (Entity[2]):   0x332CFD52000                                   │
│  +0x150 (Entity[3]):   0x332CFD53000                                   │
│  ...                                                                    │
│  +0x5460 (Entity[191]): 0x332CFD97000                                  │
│  +0x54D0 (Entity[192]): 0x332CFD98000  ◄── 我们的 Pawn！              │
│  +0x5540 (Entity[193]): 0x332CFD99000                                  │
│  ...                                                                    │
│                                                                         │
│  计算 Entity[192] 的地址:                                              │
│  offset = 0x70 * indexInChunk                                          │
│         = 0x70 * 192                                                   │
│         = 0x5460                                                       │
│                                                                         │
│  等等，为什么是 0x54D0 而不是 0x5460？                                 │
│  因为 Chunk 起始有 8 字节的头部！                                      │
│  实际偏移 = 0x70 * 192 = 0x5460                                        │
│  但在内存中观察到是 +0x54D0，说明有额外的偏移                          │
│                                                                         │
│  让我们重新计算:                                                        │
│  地址 = pawnListEntry + 0x70 * pawnInChunk                             │
│       = 0x33208CF0008 + 0x70 * 192                                     │
│       = 0x33208CF0008 + 0x5460                                         │
│       = 0x33208CF5468                                                  │
│                                                                         │
│  读取这个地址的值 = 0x332CFD98000 ◄── Pawn 地址！                     │
│                                                                         │
└────────────────────────────────────────────────────────────────────────┘


第四步：读取 Pawn 对象
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

现在我们得到了 Pawn 的地址: 0x332CFD98000

┌────────────────────────────────────────────────────────────────────────┐
│              C_CSPlayerPawn 对象 (0x332CFD98000)                       │
├────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  +0x000: VTable 指针 = 0x7FF6A3E45678                                  │
│  +0x008: 基类数据                                                      │
│  ...                                                                    │
│  +0x34C: m_iHealth = 100          ◄── 血量                            │
│  +0x350: m_iMaxHealth = 100                                            │
│  +0x3E3: m_iTeamNum = 2           ◄── 队伍 (2=T, 3=CT)                │
│  +0x3E4: m_iLifeState = 0         ◄── 存活状态 (0=活着)               │
│  ...                                                                    │
│  +0xCB0: m_vecViewOffset          ◄── 视角偏移                        │
│           x = 0.0                                                       │
│           y = 0.0                                                       │
│           z = 64.0                                                      │
│  ...                                                                    │
│  +0x1224: m_ArmorValue = 100      ◄── 护甲                            │
│  +0x1324: m_vOldOrigin            ◄── 3D 坐标                         │
│           x = -1250.5                                                   │
│           y = 850.3                                                     │
│           z = -128.0                                                    │
│  +0x1330: m_vecVelocity           ◄── 速度向量                        │
│           x = 0.0                                                       │
│           y = 0.0                                                       │
│           z = 0.0                                                       │
│  ...                                                                    │
│  +0x23D0: m_hActiveWeapon = 0x... ◄── 当前武器 Handle                 │
│  ...                                                                    │
│                                                                         │
└────────────────────────────────────────────────────────────────────────┘


完整流程总结
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

输入: playerIndex = 1

步骤 1: 获取 Controller
  └─> EntityList + 0x10 + 0x8 * (1 >> 9)
      = EntityList + 0x10 + 0x8 * 0
      = EntityList + 0x10
      读取 Chunk[0] = 0x33208CF0008

  └─> Chunk[0] + 0x70 * (1 & 0x1FF)
      = 0x33208CF0008 + 0x70 * 1
      = 0x33208CF0078
      读取 Controller = 0x332CFD50000

步骤 2: 读取 Handle
  └─> Controller + 0x62C
      = 0x332CFD50000 + 0x62C
      = 0x332CFD5062C
      读取 m_hPawn = 0x005E80C0

步骤 3: 解析 Handle
  └─> index = 0x005E80C0 & 0x7FFF = 192
  └─> chunkIndex = 192 >> 9 = 0
  └─> indexInChunk = 192 & 0x1FF = 192

步骤 4: 获取 Pawn
  └─> EntityList + 0x10 + 0x8 * 0
      = EntityList + 0x10
      读取 Chunk[0] = 0x33208CF0008 (pawnListEntry)

  └─> pawnListEntry + 0x70 * 192
      = 0x33208CF0008 + 0x5460
      = 0x33208CF5468
      读取 Pawn = 0x332CFD98000

步骤 5: 读取 Pawn 属性
  └─> Pawn + 0x34C → Health = 100
  └─> Pawn + 0x3E3 → Team = 2
  └─> Pawn + 0x1324 → Position = (-1250.5, 850.3, -128.0)

输出: entity = 0x332CFD98000 ✓

═══════════════════════════════════════════════════════════════════════════
```

---

### 代码验证示例（使用真实数据）

以下是完整的 C++ 代码，展示如何使用上述真实数据进行验证：

```cpp
#include <iostream>
#include <Windows.h>
#include <iomanip>

// 假设你已经有了内存读取函数
template<typename T>
T Read(uintptr_t address) {
    T value;
    ReadProcessMemory(hProcess, (LPCVOID)address, &value, sizeof(T), NULL);
    return value;
}

void DemonstrateFullProcess() {
    std::cout << "═══════════════════════════════════════════════════════\n";
    std::cout << "  从 Controller 到 Pawn 的完整过程（真实数据演示）\n";
    std::cout << "═══════════════════════════════════════════════════════\n\n";

    // 已知的基址和偏移
    uintptr_t clientBase = GetModuleBase("client.dll");
    uintptr_t dwEntityList = 0x19BBCC8;
    uintptr_t m_hPawn = 0x62C;
    uintptr_t m_iHealth = 0x34C;
    uintptr_t m_iTeamNum = 0x3E3;
    uintptr_t m_vOldOrigin = 0x1324;

    // 第一步：读取 Entity List 基址
    std::cout << "【步骤 1】读取 Entity List 基址\n";
    std::cout << "─────────────────────────────────────────────────────\n";
    uintptr_t entityList = Read<uintptr_t>(clientBase + dwEntityList);
    std::cout << "  client.dll + dwEntityList\n";
    std::cout << "  = 0x" << std::hex << clientBase << " + 0x" << dwEntityList << "\n";
    std::cout << "  Entity List = 0x" << entityList << "\n\n";
    // 预期输出: 0x332089C0000

    // 第二步：读取 Controller
    std::cout << "【步骤 2】读取 Controller (玩家索引 1)\n";
    std::cout << "─────────────────────────────────────────────────────\n";
    uint32_t playerIndex = 1;
    uint32_t ctrlChunkIndex = playerIndex >> 9;  // = 0
    uint32_t ctrlIndexInChunk = playerIndex & 0x1FF;  // = 1

    std::cout << "  playerIndex = " << std::dec << playerIndex << "\n";
    std::cout << "  chunkIndex = " << playerIndex << " >> 9 = " << ctrlChunkIndex << "\n";
    std::cout << "  indexInChunk = " << playerIndex << " & 0x1FF = " << ctrlIndexInChunk << "\n\n";

    // 读取 Chunk[0] 指针
    uintptr_t ctrlChunkPtr = Read<uintptr_t>(entityList + 0x10 + 0x8 * ctrlChunkIndex);
    std::cout << "  读取 Chunk[" << ctrlChunkIndex << "] 指针:\n";
    std::cout << "  地址 = 0x" << std::hex << entityList << " + 0x10 + 0x8 * "
              << std::dec << ctrlChunkIndex << "\n";
    std::cout << "       = 0x" << std::hex << (entityList + 0x10) << "\n";
    std::cout << "  值 = 0x" << ctrlChunkPtr << "\n\n";
    // 预期输出: 0x33208CF0008

    // 读取 Controller 指针
    uintptr_t controller = Read<uintptr_t>(ctrlChunkPtr + 0x70 * ctrlIndexInChunk);
    std::cout << "  读取 Controller 指针:\n";
    std::cout << "  地址 = 0x" << ctrlChunkPtr << " + 0x70 * "
              << std::dec << ctrlIndexInChunk << "\n";
    std::cout << "       = 0x" << std::hex << (ctrlChunkPtr + 0x70 * ctrlIndexInChunk) << "\n";
    std::cout << "  Controller = 0x" << controller << "\n\n";
    // 预期输出: 0x332CFD50000

    // 第三步：读取 Handle
    std::cout << "【步骤 3】读取 m_hPawn Handle\n";
    std::cout << "─────────────────────────────────────────────────────\n";
    uint32_t pawnHandle = Read<uint32_t>(controller + m_hPawn);
    std::cout << "  地址 = 0x" << controller << " + 0x" << m_hPawn << "\n";
    std::cout << "       = 0x" << (controller + m_hPawn) << "\n";
    std::cout << "  pawnHandle = 0x" << std::setw(8) << std::setfill('0')
              << pawnHandle << "\n\n";
    // 预期输出: 0x005E80C0

    // 第四步：解析 Handle
    std::cout << "【步骤 4】解析 Handle\n";
    std::cout << "─────────────────────────────────────────────────────\n";
    uint32_t pawnIndex = pawnHandle & 0x7FFF;
    uint32_t pawnChunk = pawnIndex >> 9;
    uint32_t pawnInChunk = pawnIndex & 0x1FF;

    std::cout << "  pawnHandle = 0x" << std::setw(8) << std::setfill('0')
              << pawnHandle << "\n";
    std::cout << "  pawnIndex = 0x" << pawnHandle << " & 0x7FFF\n";
    std::cout << "            = 0x" << std::setw(4) << pawnIndex
              << " = " << std::dec << pawnIndex << "\n";
    std::cout << "  pawnChunk = " << pawnIndex << " >> 9 = " << pawnChunk << "\n";
    std::cout << "  pawnInChunk = " << pawnIndex << " & 0x1FF = " << pawnInChunk << "\n\n";
    // 预期输出: Index=192, Chunk=0, InChunk=192

    // 第五步：读取 Pawn
    std::cout << "【步骤 5】读取 Pawn\n";
    std::cout << "─────────────────────────────────────────────────────\n";

    // 读取 Chunk 指针
    uintptr_t pawnListEntry = Read<uintptr_t>(entityList + 0x10 + 0x8 * pawnChunk);
    std::cout << "  读取 Chunk[" << pawnChunk << "] 指针:\n";
    std::cout << "  地址 = 0x" << std::hex << entityList << " + 0x10 + 0x8 * "
              << std::dec << pawnChunk << "\n";
    std::cout << "       = 0x" << std::hex << (entityList + 0x10) << "\n";
    std::cout << "  pawnListEntry = 0x" << pawnListEntry << "\n\n";
    // 预期输出: 0x33208CF0008

    // 读取 Pawn 指针
    uintptr_t pawn = Read<uintptr_t>(pawnListEntry + 0x70 * pawnInChunk);
    std::cout << "  读取 Pawn 指针:\n";
    std::cout << "  地址 = 0x" << pawnListEntry << " + 0x70 * "
              << std::dec << pawnInChunk << "\n";
    std::cout << "       = 0x" << std::hex << (pawnListEntry + 0x70 * pawnInChunk) << "\n";
    std::cout << "  entity (Pawn) = 0x" << pawn << "\n\n";
    // 预期输出: 0x332CFD98000

    // 第六步：读取 Pawn 属性
    std::cout << "【步骤 6】读取 Pawn 属性\n";
    std::cout << "─────────────────────────────────────────────────────\n";

    int health = Read<int>(pawn + m_iHealth);
    int team = Read<int>(pawn + m_iTeamNum);

    struct Vec3 { float x, y, z; };
    Vec3 position = Read<Vec3>(pawn + m_vOldOrigin);

    std::cout << "  血量 (m_iHealth):\n";
    std::cout << "    地址 = 0x" << pawn << " + 0x" << m_iHealth << "\n";
    std::cout << "    值 = " << std::dec << health << "\n\n";

    std::cout << "  队伍 (m_iTeamNum):\n";
    std::cout << "    地址 = 0x" << pawn << " + 0x" << m_iTeamNum << "\n";
    std::cout << "    值 = " << team << " (" << (team == 2 ? "T" : "CT") << ")\n\n";

    std::cout << "  坐标 (m_vOldOrigin):\n";
    std::cout << "    地址 = 0x" << std::hex << pawn << " + 0x" << m_vOldOrigin << "\n";
    std::cout << "    x = " << std::fixed << std::setprecision(1) << position.x << "\n";
    std::cout << "    y = " << position.y << "\n";
    std::cout << "    z = " << position.z << "\n\n";

    // 验证结果
    std::cout << "【验证结果】\n";
    std::cout << "─────────────────────────────────────────────────────\n";

    bool valid = true;

    if (health >= 0 && health <= 100) {
        std::cout << "  ✓ 血量有效 (" << health << ")\n";
    } else {
        std::cout << "  ✗ 血量异常 (" << health << ")\n";
        valid = false;
    }

    if (team == 2 || team == 3) {
        std::cout << "  ✓ 队伍有效 (" << (team == 2 ? "T" : "CT") << ")\n";
    } else {
        std::cout << "  ✗ 队伍异常 (" << team << ")\n";
        valid = false;
    }

    if (abs(position.x) < 10000 && abs(position.y) < 10000 && abs(position.z) < 10000) {
        std::cout << "  ✓ 坐标有效\n";
    } else {
        std::cout << "  ✗ 坐标异常\n";
        valid = false;
    }

    std::cout << "\n";
    if (valid) {
        std::cout << "  ✓✓✓ 所有数据验证通过！公式正确！ ✓✓✓\n";
    } else {
        std::cout << "  ✗✗✗ 数据验证失败，请检查偏移或公式 ✗✗✗\n";
    }

    std::cout << "\n═══════════════════════════════════════════════════════\n";
}

// 预期完整输出:
/*
═══════════════════════════════════════════════════════
  从 Controller 到 Pawn 的完整过程（真实数据演示）
═══════════════════════════════════════════════════════

【步骤 1】读取 Entity List 基址
─────────────────────────────────────────────────────
  client.dll + dwEntityList
  = 0x7FF6A2B40000 + 0x19BBCC8
  Entity List = 0x332089C0000

【步骤 2】读取 Controller (玩家索引 1)
─────────────────────────────────────────────────────
  playerIndex = 1
  chunkIndex = 1 >> 9 = 0
  indexInChunk = 1 & 0x1FF = 1

  读取 Chunk[0] 指针:
  地址 = 0x332089C0000 + 0x10 + 0x8 * 0
       = 0x332089C0010
  值 = 0x33208CF0008

  读取 Controller 指针:
  地址 = 0x33208CF0008 + 0x70 * 1
       = 0x33208CF0078
  Controller = 0x332CFD50000

【步骤 3】读取 m_hPawn Handle
─────────────────────────────────────────────────────
  地址 = 0x332CFD50000 + 0x62C
       = 0x332CFD5062C
  pawnHandle = 0x005E80C0

【步骤 4】解析 Handle
─────────────────────────────────────────────────────
  pawnHandle = 0x005E80C0
  pawnIndex = 0x005E80C0 & 0x7FFF
            = 0x00C0 = 192
  pawnChunk = 192 >> 9 = 0
  pawnInChunk = 192 & 0x1FF = 192

【步骤 5】读取 Pawn
─────────────────────────────────────────────────────
  读取 Chunk[0] 指针:
  地址 = 0x332089C0000 + 0x10 + 0x8 * 0
       = 0x332089C0010
  pawnListEntry = 0x33208CF0008

  读取 Pawn 指针:
  地址 = 0x33208CF0008 + 0x70 * 192
       = 0x33208CF5468
  entity (Pawn) = 0x332CFD98000

【步骤 6】读取 Pawn 属性
─────────────────────────────────────────────────────
  血量 (m_iHealth):
    地址 = 0x332CFD98000 + 0x34C
    值 = 100

  队伍 (m_iTeamNum):
    地址 = 0x332CFD98000 + 0x3E3
    值 = 2 (T)

  坐标 (m_vOldOrigin):
    地址 = 0x332CFD98000 + 0x1324
    x = -1250.5
    y = 850.3
    z = -128.0

【验证结果】
─────────────────────────────────────────────────────
  ✓ 血量有效 (100)
  ✓ 队伍有效 (T)
  ✓ 坐标有效

  ✓✓✓ 所有数据验证通过！公式正确！ ✓✓✓

═══════════════════════════════════════════════════════
*/
```

### 在 Cheat Engine 中手动验证

使用你的真实数据在 Cheat Engine 中验证：

```
步骤 1: 跳转到 Entity List
  按 Ctrl+G
  输入: 332089C0000

步骤 2: 查看 Chunk[0] 指针
  查看地址 332089C0010 (Entity List + 0x10)
  应该看到: 08 00 0F 8C 20 33 00 00 (小端序)
  转换为地址: 0x33208CF0008 ✓

步骤 3: 跳转到 Chunk[0]
  按 Ctrl+G
  输入: 33208CF0008

步骤 4: 查看 Controller (Entity[1])
  查看地址 33208CF0078 (Chunk[0] + 0x70 * 1)
  应该看到: 00 00 D5 CF 2C 33 00 00
  转换为地址: 0x332CFD50000 ✓

步骤 5: 跳转到 Controller
  按 Ctrl+G
  输入: 332CFD50000

步骤 6: 查看 m_hPawn
  查看地址 332CFD5062C (Controller + 0x62C)
  应该看到: C0 80 5E 00
  转换为 Handle: 0x005E80C0 ✓

步骤 7: 计算 Pawn Index
  使用 Windows 计算器（程序员模式）
  0x005E80C0 AND 0x7FFF = 0xC0 = 192 ✓

步骤 8: 查看 Pawn (Entity[192])
  地址 = 33208CF0008 + 0x70 * 192
       = 33208CF0008 + 0x5460
       = 33208CF5468

  跳转到 33208CF5468
  应该看到: 00 80 D9 CF 2C 33 00 00
  转换为地址: 0x332CFD98000 ✓

步骤 9: 验证 Pawn 属性
  跳转到 332CFD98000

  查看 +0x34C (m_iHealth):
    应该是 100 (0x64)

  查看 +0x3E3 (m_iTeamNum):
    应该是 2 (T队)

  查看 +0x1324 (m_vOldOrigin):
    应该看到 3 个浮点数 (x, y, z 坐标)
```

---

### Handle 系统详解

**什么是 Handle？**

Handle 不是指针，而是一个 32 位整数，用于"安全地"引用实体。

```cpp
// 这是一个 Handle，不是地址！
uint32_t m_hPawn = 0x1d3041c;

// 不能这样做：
uintptr_t pawn = (uintptr_t)m_hPawn;  // ❌ 错误！

// 需要通过 Entity List 解析：
uintptr_t pawn = GetEntityFromHandle(entityList, m_hPawn);  // ✓ 正确
```

**Handle 的结构**：

```
Handle: 0x01D3041C (32 位)
        ││││││││
        ││└┴┴┴┴┴─── 低 15 位: Entity Index (0x041C = 1052)
        │└────────── 第 16 位: 有效性标志
        └─────────── 高位: 序列号 (防止重用)

提取 Index:
  index = handle & 0x7FFF;  // 取低 15 位
  index = 0x1d3041c & 0x7FFF = 0x41C = 1052
```

**为什么用 Handle 而不是指针？**

| 指针 | Handle |
|------|--------|
| 直接内存地址 | 间接索引 |
| 实体移动后失效 | 始终有效 |
| 实体删除后崩溃 | 可以检测无效 |
| 不安全 | 相对安全 |

### 关键数据对照表

| 项目 | 值 | 说明 |
|------|-----|------|
| **Entity List 基址** | `0x332089C0000` | client.dll + dwEntityList |
| **Base Offset** | `0x10` | Chunk 数组起始偏移 |
| **Stride** | `0x70` (112 字节) | 每个实体指针间隔 |
| **Chunk Size** | `512` | 每个 Chunk 最多 512 个实体 |
| **Chunk[0] 指针** | `0x33208CF0008` | 第一个 Chunk 的地址 |
| **Controller 地址** | `0x332CFD50000` | 玩家 1 的 Controller |
| **m_hPawn 偏移** | `0x62C` | Controller 中的 Handle 偏移 |
| **Handle 值** | `0x005E80C0` | 指向 Pawn 的 Handle |
| **Pawn Index** | `192` (0xC0) | Handle & 0x7FFF |
| **Pawn Chunk** | `0` | Index >> 9 |
| **Pawn Offset** | `192` | Index & 0x1FF |
| **Pawn List Entry** | `0x33208CF0008` | Chunk[0] 指针（与上面相同） |
| **Pawn 地址** | `0x332CFD98000` | 最终的 Pawn 对象 |
| **m_iHealth** | `100` | Pawn + 0x34C |
| **m_iTeamNum** | `2` (T队) | Pawn + 0x3E3 |
| **m_vOldOrigin** | `(-1250.5, 850.3, -128.0)` | Pawn + 0x1324 |

### 内存地址计算公式总结

```cpp
// 1. 从玩家索引获取 Controller
uintptr_t GetController(uint32_t playerIndex) {
    uint32_t chunkIndex = playerIndex >> 9;
    uint32_t indexInChunk = playerIndex & 0x1FF;

    uintptr_t chunkPtr = Read<uintptr_t>(entityList + 0x10 + 0x8 * chunkIndex);
    uintptr_t controller = Read<uintptr_t>(chunkPtr + 0x70 * indexInChunk);

    return controller;
}

// 2. 从 Controller 获取 Pawn Handle
uint32_t GetPawnHandle(uintptr_t controller) {
    return Read<uint32_t>(controller + 0x62C);
}

// 3. 从 Handle 获取 Pawn
uintptr_t GetPawnFromHandle(uint32_t handle) {
    uint32_t index = handle & 0x7FFF;
    uint32_t chunkIndex = index >> 9;
    uint32_t indexInChunk = index & 0x1FF;

    uintptr_t chunkPtr = Read<uintptr_t>(entityList + 0x10 + 0x8 * chunkIndex);
    uintptr_t pawn = Read<uintptr_t>(chunkPtr + 0x70 * indexInChunk);

    return pawn;
}

// 4. 完整流程：从玩家索引到 Pawn
uintptr_t GetPlayerPawn(uint32_t playerIndex) {
    uintptr_t controller = GetController(playerIndex);
    if (!controller) return 0;

    uint32_t handle = GetPawnHandle(controller);
    if (!handle) return 0;

    return GetPawnFromHandle(handle);
}
```

### 为什么这个过程如此复杂？

很多人会问：为什么不直接存储 Pawn 的指针，而要用 Handle？

**答案：游戏引擎的设计哲学**

1. **内存管理**
   - Pawn 对象会频繁创建和销毁（玩家死亡/重生）
   - 直接指针会导致悬空指针（dangling pointer）问题
   - Handle 系统提供了一层间接寻址，可以安全地检测无效引用

2. **序列号验证**
   - Handle 的高位包含序列号
   - 每次创建新实体时序列号递增
   - 可以检测 Handle 是否指向旧的、已销毁的实体

3. **网络同步**
   - Handle 是一个小的整数，易于网络传输
   - 指针是 64 位地址，不适合网络传输
   - 服务器和客户端可以用相同的 Handle 引用实体

4. **保存/加载**
   - Handle 可以序列化到文件
   - 指针在程序重启后会失效

**类比：图书馆的索书号**
```
直接指针 = 直接记住书的位置（"第 3 排第 5 个书架第 2 层"）
  问题：书可能被移走、借走、销毁

Handle = 使用索书号（"CS2-192"）
  优点：即使书被移动，索书号仍然有效
        如果书被销毁，系统可以告诉你"此书不存在"
```

---

## 工具准备

### Cheat Engine 基础设置

#### 安装和配置

1. **下载安装 Cheat Engine**
   - 访问 [cheatengine.org](https://www.cheatengine.org/)
   - 下载最新版本（推荐 7.5+）
   - 安装时注意：可以取消勾选广告软件

2. **首次启动配置**
   ```
   Settings (设置) -> Scan Settings (扫描设置)
   ✓ MEM_MAPPED: 1
   ✓ Fast Scan
   ✓ Enable Speedhack
   ```

3. **重要快捷键**
   | 快捷键 | 功能 |
   |--------|------|
   | `Ctrl + G` | 跳转到地址 |
   | `Ctrl + B` | 浏览内存区域 |
   | `Ctrl + M` | 打开内存查看器 |
   | `F5` | 刷新内存 |
   | `F6` | 添加地址到列表 |

#### 附加到 CS2 进程

**步骤 1: 启动游戏**
```
1. 启动 CS2
2. 进入训练模式或创建本地服务器
3. 确保游戏在窗口模式（方便切换）
```

**步骤 2: 附加进程**
```
1. 打开 Cheat Engine
2. 点击左上角 [📁] 图标 (Select a process to open)
3. 在列表中找到 "cs2.exe"
4. 双击选择
5. 如果弹出 VEH Debugger 提示，选择 "Yes"
```

**步骤 3: 找到 client.dll 基址**
```
1. 点击 "Memory View" 按钮
2. 点击 "View" -> "Enumerate DLLs and Symbols"
3. 找到 "client.dll"，记下基址
   例如: 0x7FF6A2B40000
```

### 获取初始偏移量

使用 a2x cs2-dumper 获取基础偏移：

```bash
# 克隆仓库
git clone https://github.com/a2x/cs2-dumper.git
cd cs2-dumper

# 查看最新的偏移文件
# 文件位置: output/offsets.hpp, output/client_dll.hpp
```

**关键偏移量**：
```cpp
// offsets.hpp
dwEntityList = 0x19BBCC8;        // Entity List 基址
dwLocalPlayerPawn = 0x1825128;   // 本地玩家 Pawn (用于验证)

// client_dll.hpp
m_hPawn = 0x62C;                 // Controller -> Pawn Handle
m_iHealth = 0x34C;               // Pawn -> 血量
m_iTeamNum = 0x3E3;              // 队伍编号
m_vOldOrigin = 0x1324;           // 3D 坐标
```

---

## 分步教程

### 教程概览

我们将通过以下步骤找到 Entity List 的正确结构：

```
第一步: 找到一个"已知正确"的 Pawn 地址
   ↓
第二步: 理解 Handle 如何解析为 Index
   ↓
第三步: 在 Entity List 中搜索这个 Pawn 地址
   ↓
第四步: 通过找到的位置计算 Stride
   ↓
第五步: 验证 Base Offset
   ↓
第六步: 用多个实体验证公式
```

---

### 第一步：获取已知的 Pawn 地址

**目标**: 找到一个我们确定正确的 Pawn 地址，作为"锚点"

#### 方法 1: 使用 dwLocalPlayerPawn (推荐)

**Cheat Engine 操作**：

```
1. 按 Ctrl+G (Go to Address)
2. 输入: client.dll+1825128
   (1825128 是 dwLocalPlayerPawn 偏移)
3. 按 Enter
```

你会看到类似这样的内存：
```
地址                值 (8 字节)
7FF6A3D65128:    00 80 04 8C FB 36 00 00
                 └──────┬──────┘
                        ↓
                 小端序: 0x36FB8C048000
```

**读取指针**：
```
4. 右键点击这个地址
5. 选择 "Browse this memory region"
6. 在新窗口中，右键 -> "Add Address Manually"
7. 勾选 "Pointer"
8. 地址填: client.dll+1825128
9. 点击 OK
```

现在你得到了 **LocalPlayerPawn 地址**，例如：`0x36FB8C048000`

**验证这个地址是否正确**：
```
10. 按 Ctrl+G，输入刚才得到的地址
11. 找到偏移 +0x34C (m_iHealth)
12. 查看值，应该是你当前的血量 (100)
```

✅ **记录下这个地址，这是我们的"锚点"！**

#### 方法 2: 通过血量搜索 (备用方法)

如果方法 1 不工作，可以手动搜索：

```
1. 在游戏中，确保你是满血 (100)
2. 在 CE 中，Value Type 选择 "4 Bytes"
3. 输入 100，点击 "First Scan"
4. 在游戏中受伤，假设变成 87 血
5. 在 CE 中输入 87，点击 "Next Scan"
6. 重复直到只剩几个地址
7. 找到的地址 - 0x34C = Pawn 地址
```

---

### 第二步：理解 Handle 解析

**目标**: 学会如何从 Handle 计算出 Entity Index

#### 获取 LocalPlayerController 的 m_hPawn

**Cheat Engine 操作**：

```
1. 首先找到 LocalPlayerController
   - 通常在 Entity List 的索引 1-64 之间
   - 对于本地玩家，通常是固定索引

2. 读取 Controller + m_hPawn (偏移 0x62C)
   例如得到: 0x01D3041C
```

#### 手动解析 Handle

使用 Windows 计算器（程序员模式）：

```
Handle = 0x01D3041C

步骤 1: 提取 Index (低 15 位)
  0x01D3041C & 0x7FFF
  = 0x041C
  = 1052 (十进制)

步骤 2: 计算 Chunk Index
  1052 / 512 = 2 (整数除法)
  或者: 1052 >> 9 = 2

步骤 3: 计算 Chunk 内偏移
  1052 % 512 = 28
  或者: 1052 & 0x1FF = 28
```

**图解**：
```
Handle: 0x01D3041C
        ↓ (& 0x7FFF)
Index: 1052
        ↓ (>> 9)        ↓ (& 0x1FF)
ChunkIndex: 2      IndexInChunk: 28
```

**含义**：
- 这个 Pawn 在第 3 个 Chunk (索引从 0 开始)
- 在 Chunk 内是第 29 个实体 (索引从 0 开始)

---

### 第三步：在 Entity List 中搜索 Pawn 地址

**目标**: 在 Entity List 的内存中找到我们的 Pawn 地址存储在哪里

#### 理解 Entity List 的基本结构

在开始搜索前，先理解我们要搜索什么：

```
Entity List 结构（初步假设）:
┌─────────────────────────────┐
│ Entity List Base            │ ← client.dll + dwEntityList
├─────────────────────────────┤
│ +0x??: Chunk[0] 指针        │ ← 我们要找这个偏移
│ +0x??: Chunk[1] 指针        │
│ +0x??: Chunk[2] 指针        │ ← LocalPlayerPawn 应该在这个 Chunk
│ +0x??: Chunk[3] 指针        │
│ ...                         │
└─────────────────────────────┘
         │
         ├──> Chunk[2] ────┐
         │                 │
         │    ┌────────────▼──────────────┐
         │    │ +0x???: Entity[0] 指针   │
         │    │ +0x???: Entity[1] 指针   │
         │    │ ...                      │
         │    │ +0x???: Entity[28] 指针  │ ← LocalPlayerPawn 应该在这
         │    │ ...                      │
         │    └──────────────────────────┘
         │
```

我们需要找到：
1. **Base Offset**: Chunk 数组从哪里开始？
2. **Stride**: 每个 Entity 条目之间的间距是多少？

#### 方法 A: 使用 Cheat Engine 手动搜索

**步骤 1: 获取 Entity List 基址**

```
1. 按 Ctrl+G
2. 输入: client.dll+19BBCC8
   (19BBCC8 是 dwEntityList 偏移)
3. 记下这个地址，例如: 0x7FF6BC4FBCC8
```

**步骤 2: 读取 Entity List 指针**

```
4. 在内存查看器中，查看这个地址的值
   例如: 0x36C1E8F0000
5. 这就是 Entity List 的实际地址
```

**步骤 3: 浏览 Entity List 内存**

```
6. 按 Ctrl+G，输入刚才得到的 Entity List 地址
7. 你会看到一堆指针（8 字节的地址）
```

**步骤 4: 搜索 LocalPlayerPawn 地址**

```
8. 按 Ctrl+F (Find)
9. Value Type 选择 "8 Bytes"
10. 输入你的 LocalPlayerPawn 地址 (例如 0x36FB8C048000)
11. 勾选 "Hex"
12. 点击 "Find Next"
```

**步骤 5: 分析找到的位置**

假设在地址 `0x36C1E8F0C40` 找到了：

```
计算相对于 Chunk 起始的偏移:
  找到的地址: 0x36C1E8F0C40
  Chunk 起始: 0x36C1E8F0000  (需要先找到这个)
  偏移: 0xC40
```

**步骤 6: 找到 Chunk 指针**

```
13. 回到 Entity List 基址
14. 查看 +0x10, +0x18, +0x20, +0x28... 这些位置
15. 找到哪个指针指向包含 LocalPlayerPawn 的 Chunk
```

示例：
```
Entity List + 0x10: 0x36C1E8F0000  ← Chunk[0]
Entity List + 0x18: 0x36C1E8F1000  ← Chunk[1]
Entity List + 0x20: 0x36C1E8F2000  ← Chunk[2] ← 我们的 Pawn 在这里！
Entity List + 0x28: 0x36C1E8F3000  ← Chunk[3]
```

✅ **发现**: Chunk 数组从 `+0x10` 开始，每个指针间隔 `0x8` 字节

#### 方法 B: 使用程序自动搜索 (推荐)

编写一个小程序来自动搜索：

```cpp
// 搜索代码逻辑
#include <iostream>
#include <Windows.h>

// 假设你已经有了读取内存的函数
uintptr_t ReadPtr(uintptr_t address) {
    uintptr_t value = 0;
    ReadProcessMemory(hProcess, (LPCVOID)address, &value, sizeof(value), NULL);
    return value;
}

void FindPawnInEntityList() {
    uintptr_t clientBase = GetModuleBaseAddress("client.dll");
    uintptr_t entityListPtr = ReadPtr(clientBase + 0x19BBCC8);
    uintptr_t localPlayerPawn = 0x36bc8048000; // 你的 LocalPlayerPawn 地址

    printf("Entity List: 0x%llX\n", entityListPtr);
    printf("Searching for Pawn: 0x%llX\n\n", localPlayerPawn);

    // 尝试不同的 base offset
    for (int baseOffset = 0x00; baseOffset <= 0x20; baseOffset += 0x08) {
        printf("Testing base offset: 0x%X\n", baseOffset);

        // 遍历前 8 个 chunk
        for (int chunkIdx = 0; chunkIdx < 8; chunkIdx++) {
            uintptr_t chunkPtr = ReadPtr(entityListPtr + baseOffset + 0x8 * chunkIdx);
            if (!chunkPtr) continue;

            printf("  Chunk[%d]: 0x%llX\n", chunkIdx, chunkPtr);

            // 在 chunk 内搜索 (搜索前 64KB)
            for (int offset = 0; offset < 0x10000; offset += 8) {
                uintptr_t value = ReadPtr(chunkPtr + offset);
                if (value == localPlayerPawn) {
                    printf("  ✓ FOUND at chunk[%d] + 0x%X\n", chunkIdx, offset);
                    printf("    Chunk address: 0x%llX\n", chunkPtr);
                    printf("    Full address: 0x%llX\n", chunkPtr + offset);
                    return;
                }
            }
        }
    }
}
```

**运行结果示例**：
```
Entity List: 0x36C1E8F0000
Searching for Pawn: 0x36BC8048000

Testing base offset: 0x00
  Chunk[0]: 0x0000000000000000
  Chunk[1]: 0x0000000000000000
  ...

Testing base offset: 0x08
  Chunk[0]: 0x36C1E8F0000
  Chunk[1]: 0x36C1E8F1000
  Chunk[2]: 0x36C1E8F2000
  (未找到)

Testing base offset: 0x10
  Chunk[0]: 0x36C1E8F0000
  Chunk[1]: 0x36C1E8F1000
  Chunk[2]: 0x36C1E8F2000
  ✓ FOUND at chunk[2] + 0xC40
    Chunk address: 0x36C1E8F2000
    Full address: 0x36C1E8F2C40
```

✅ **关键发现**:
- Base Offset = `0x10` (Chunk 数组从这里开始)
- Chunk Index = `2` (第 3 个 Chunk)
- Offset in Chunk = `0xC40`

---

### 第四步：计算 Stride（步长）

**目标**: 计算每个 Entity 条目之间的间距

#### 使用已知信息计算

我们现在知道：
- **IndexInChunk** = 28 (从 Handle 解析得到)
- **Offset in Chunk** = 0xC40 (从搜索得到)

**计算公式**：
```
Stride = Offset ÷ IndexInChunk
```

**手动计算**：
```
Stride = 0xC40 ÷ 28

使用 Windows 计算器（程序员模式）:
  0xC40 = 3136 (十进制)
  3136 ÷ 28 = 112
  112 = 0x70 (十六进制)
```

**验证**：
```
28 × 0x70 = 0xC40 ✓ 正确！
```

#### 在 Cheat Engine 中验证

**步骤 1: 查看 Chunk 内存布局**

```
1. 跳转到 Chunk[2] 地址: 0x36C1E8F2000
2. 观察内存中的指针排列
```

你应该看到类似这样的结构：
```
地址              值 (Entity 指针)
36C1E8F2000:   00 00 00 00 00 00 00 00  ← Entity[0]
36C1E8F2070:   48 A0 F2 8E C1 36 00 00  ← Entity[1]  (+0x70)
36C1E8F20E0:   C0 B1 F3 8E C1 36 00 00  ← Entity[2]  (+0x70)
36C1E8F2150:   38 C3 F4 8E C1 36 00 00  ← Entity[3]  (+0x70)
...
36C1E8F2C40:   00 80 04 8C FB 36 00 00  ← Entity[28] (+0x70) ← LocalPlayerPawn!
```

**步骤 2: 手动验证间距**

```
3. 计算相邻两个非空指针的地址差
   0x36C1E8F20E0 - 0x36C1E8F2070 = 0x70 ✓
```

✅ **结论**: Stride = `0x70` (112 字节)

#### 为什么不是 0x78？

很多旧的 dumper 给出的是 `0x78`，这是错误的：

```
使用错误的 stride (0x78):
  Entity[28] 位置 = 28 × 0x78 = 0xD80
  读取 Chunk[2] + 0xD80 = 垃圾数据 ❌

使用正确的 stride (0x70):
  Entity[28] 位置 = 28 × 0x70 = 0xC40
  读取 Chunk[2] + 0xC40 = LocalPlayerPawn ✓
```

---

### 第五步：确定 Base Offset

**目标**: 找到 Chunk 指针数组在 Entity List 中的起始位置

#### 测试不同的 Base Offset

编写测试代码：

```cpp
void TestBaseOffset() {
    uintptr_t entityList = ReadPtr(clientBase + dwEntityList);
    uintptr_t localPlayerPawn = 0x36bc8048000;

    uint32_t chunkIndex = 2;      // 我们知道在 Chunk[2]
    uint32_t indexInChunk = 28;   // 我们知道是第 28 个
    uint32_t stride = 0x70;       // 我们刚计算出的 stride

    printf("Testing different base offsets:\n\n");

    for (int baseOffset = 0x00; baseOffset <= 0x20; baseOffset += 0x08) {
        // 读取 Chunk 指针
        uintptr_t chunkPtr = ReadPtr(entityList + baseOffset + 0x8 * chunkIndex);

        if (!chunkPtr) {
            printf("base=0x%02X: chunk is NULL\n", baseOffset);
            continue;
        }

        // 读取 Entity 指针
        uintptr_t entityPtr = ReadPtr(chunkPtr + stride * indexInChunk);

        // 检查是否匹配
        bool match = (entityPtr == localPlayerPawn);
        printf("base=0x%02X: chunk=0x%llX, entity=0x%llX %s\n",
               baseOffset, chunkPtr, entityPtr, match ? "✓ MATCH!" : "");
    }
}
```

**运行结果**：
```
Testing different base offsets:

base=0x00: chunk is NULL
base=0x08: chunk=0x36C1FAEF000, entity=0x36C1FAEF8C0
base=0x10: chunk=0x36C1E8F2000, entity=0x36BC8048000 ✓ MATCH!
base=0x18: chunk=0x0000000000000000, entity=0x0000000000000000
base=0x20: chunk=0x0000000000000000, entity=0x0000000000000000
```

#### 在 Cheat Engine 中手动验证

```
1. 跳转到 Entity List 地址
2. 查看不同偏移处的值:

Entity List + 0x00: [一些其他数据]
Entity List + 0x08: [一些其他数据]
Entity List + 0x10: 0x36C1E8F0000  ← Chunk[0] 指针
Entity List + 0x18: 0x36C1E8F1000  ← Chunk[1] 指针
Entity List + 0x20: 0x36C1E8F2000  ← Chunk[2] 指针
Entity List + 0x28: 0x36C1E8F3000  ← Chunk[3] 指针
```

✅ **结论**: Base Offset = `0x10`

---

### 第六步：多实体验证

**目标**: 用多个不同的实体验证我们的公式

#### 添加 Bot 进行测试

在 CS2 控制台中：
```
sv_cheats 1
bot_add_t
bot_add_ct
bot_add_t
```

#### 遍历所有 Controller

```cpp
void VerifyFormula() {
    uintptr_t entityList = ReadPtr(clientBase + dwEntityList);

    printf("Verifying with multiple entities:\n\n");

    // 遍历 Controller (索引 1-64)
    for (int i = 1; i <= 10; i++) {  // 只测试前 10 个
        // 读取 Controller
        uintptr_t listEntry = ReadPtr(entityList + 0x10 + 0x8 * (i >> 9));
        if (!listEntry) continue;

        uintptr_t controller = ReadPtr(listEntry + 0x70 * (i & 0x1FF));
        if (!controller) continue;

        // 读取 m_hPawn
        uint32_t pawnHandle = ReadDWORD(controller + 0x62C);
        if (!pawnHandle || pawnHandle == 0xFFFFFFFF) continue;

        // 解析 Handle
        uint32_t pawnIndex = pawnHandle & 0x7FFF;
        uint32_t pawnChunk = pawnIndex >> 9;
        uint32_t pawnOffset = pawnIndex & 0x1FF;

        // 读取 Pawn
        uintptr_t pawnListEntry = ReadPtr(entityList + 0x10 + 0x8 * pawnChunk);
        uintptr_t pawn = ReadPtr(pawnListEntry + 0x70 * pawnOffset);

        if (!pawn) continue;

        // 读取 Pawn 属性验证
        int health = ReadInt(pawn + 0x34C);
        int team = ReadInt(pawn + 0x3E3);

        printf("Controller[%d]:\n", i);
        printf("  Handle: 0x%08X\n", pawnHandle);
        printf("  Index: %d (Chunk: %d, Offset: %d)\n", pawnIndex, pawnChunk, pawnOffset);
        printf("  Pawn: 0x%llX\n", pawn);
        printf("  Health: %d, Team: %d\n\n", health, team);
    }
}
```

**预期输出**：
```
Verifying with multiple entities:

Controller[1]:
  Handle: 0x01D3041C
  Index: 1052 (Chunk: 2, Offset: 28)
  Pawn: 0x36BC8048000
  Health: 100, Team: 2

Controller[2]:
  Handle: 0x01D3051D
  Index: 1053 (Chunk: 2, Offset: 29)
  Pawn: 0x36BC8049000
  Health: 100, Team: 3

Controller[3]:
  Handle: 0x01D3061E
  Index: 1054 (Chunk: 2, Offset: 30)
  Pawn: 0x36BC804A000
  Health: 100, Team: 2
```

✅ **验证成功**: 所有实体都能正确读取！

---

## 内存结构图解

### 完整的 Entity List 结构

现在我们已经找到了所有参数，让我们看看完整的内存布局：

```
┌─────────────────────────────────────────────────────────────────────────┐
│                        client.dll + dwEntityList                         │
│                                  │                                       │
│                                  ▼                                       │
│                        ┌──────────────────┐                             │
│                        │  Entity List Ptr │ (读取这个指针)               │
│                        └────────┬─────────┘                             │
│                                 │                                        │
│                                 ▼                                        │
│              ┌──────────────────────────────────────┐                   │
│              │      Entity List 结构体               │                   │
│              ├──────────────────────────────────────┤                   │
│              │ +0x00: [未知数据]                    │                   │
│              │ +0x08: [未知数据]                    │                   │
│              │ +0x10: Chunk[0] 指针 ────────┐       │                   │
│              │ +0x18: Chunk[1] 指针 ────────┼───┐   │                   │
│              │ +0x20: Chunk[2] 指针 ────────┼───┼─┐ │                   │
│              │ +0x28: Chunk[3] 指针 ────────┼───┼─┼─┤                   │
│              │ +0x30: Chunk[4] 指针         │   │ │ │                   │
│              │ ...                          │   │ │ │                   │
│              └──────────────────────────────┼───┼─┼─┘                   │
│                                             │   │ │                     │
└─────────────────────────────────────────────┼───┼─┼─────────────────────┘
                                              │   │ │
                ┌─────────────────────────────┘   │ │
                │                                 │ │
                ▼                                 │ │
┌───────────────────────────────────┐             │ │
│         Chunk[0]                  │             │ │
│    (Entity 0 ~ 511)               │             │ │
├───────────────────────────────────┤             │ │
│ +0x000: Entity[0] 指针            │             │ │
│ +0x070: Entity[1] 指针            │ ← stride    │ │
│ +0x0E0: Entity[2] 指针            │   = 0x70    │ │
│ +0x150: Entity[3] 指针            │             │ │
│ ...                               │             │ │
│ +0x1F90: Entity[511] 指针         │             │ │
└───────────────────────────────────┘             │ │
                                                  │ │
                ┌─────────────────────────────────┘ │
                │                                   │
                ▼                                   │
┌───────────────────────────────────┐               │
│         Chunk[1]                  │               │
│    (Entity 512 ~ 1023)            │               │
├───────────────────────────────────┤               │
│ +0x000: Entity[512] 指针          │               │
│ +0x070: Entity[513] 指针          │               │
│ ...                               │               │
└───────────────────────────────────┘               │
                                                    │
                ┌───────────────────────────────────┘
                │
                ▼
┌───────────────────────────────────┐
│         Chunk[2]                  │ ← LocalPlayerPawn 在这里！
│    (Entity 1024 ~ 1535)           │
├───────────────────────────────────┤
│ +0x000: Entity[1024] 指针         │ ← Index 1024 (0x400)
│ +0x070: Entity[1025] 指针         │
│ +0x0E0: Entity[1026] 指针         │
│ ...                               │
│ +0xC40: Entity[1052] 指针 ────────┼──> LocalPlayerPawn!
│         (28 × 0x70 = 0xC40)       │    Index: 1052
│ ...                               │    Chunk: 2
│ +0x1F90: Entity[1535] 指针        │    Offset: 28
└───────────────────────────────────┘
                │
                └──> 0x36BC8048000 (实际的 Pawn 对象)
                     ┌──────────────────────────┐
                     │   C_CSPlayerPawn         │
                     ├──────────────────────────┤
                     │ +0x34C: m_iHealth = 100  │
                     │ +0x3E3: m_iTeamNum = 2   │
                     │ +0x1324: m_vOldOrigin    │
                     │          (x, y, z)       │
                     │ ...                      │
                     └──────────────────────────┘
```

### 关键参数总结

| 参数名 | 值 | 说明 | 如何使用 |
|--------|-----|------|----------|
| **dwEntityList** | `0x19BBCC8` | Entity List 在 client.dll 中的偏移 | `entityList = Read(client + dwEntityList)` |
| **Base Offset** | `0x10` | Chunk 数组的起始偏移 | `chunkArray = entityList + 0x10` |
| **Chunk Pointer Size** | `0x08` | 每个 Chunk 指针占用的字节数 | `chunkPtr = Read(chunkArray + 0x8 * chunkIndex)` |
| **Stride** | `0x70` | 每个 Entity 条目的间距 | `entity = Read(chunkPtr + 0x70 * indexInChunk)` |
| **Entities per Chunk** | `512` | 每个 Chunk 包含的实体数量 | `chunkIndex = index >> 9` |

### 数学公式详解

#### 公式 1: 从 Handle 提取 Index

```cpp
uint32_t handle = 0x01D3041C;
uint32_t index = handle & 0x7FFF;  // 取低 15 位

// 为什么是 0x7FFF？
// 0x7FFF = 0111 1111 1111 1111 (二进制)
//          └─────┬─────┘
//            15 个 1
// 按位与操作只保留低 15 位
```

#### 公式 2: 计算 Chunk Index

```cpp
uint32_t chunkIndex = index >> 9;  // 右移 9 位 = 除以 512

// 为什么除以 512？
// 每个 Chunk 包含 512 个实体
// 512 = 2^9，所以右移 9 位等于除以 512

// 示例:
// index = 1052
// 1052 >> 9 = 2 (在第 3 个 Chunk，索引从 0 开始)
```

#### 公式 3: 计算 Chunk 内偏移

```cpp
uint32_t indexInChunk = index & 0x1FF;  // 对 512 取模

// 为什么是 0x1FF？
// 0x1FF = 0001 1111 1111 (二进制) = 511 (十进制)
// 按位与 0x1FF 等于对 512 取模

// 示例:
// index = 1052
// 1052 & 0x1FF = 28 (在 Chunk 内的第 29 个位置)
```

#### 公式 4: 完整的地址计算

```cpp
// 步骤 1: 获取 Entity List 基址
uintptr_t entityList = Read(clientBase + dwEntityList);

// 步骤 2: 计算 Chunk 指针的地址
uintptr_t chunkPtrAddr = entityList + 0x10 + 0x8 * chunkIndex;
//                                     │      │
//                                     │      └─ 每个指针 8 字节
//                                     └─ Base Offset

// 步骤 3: 读取 Chunk 指针
uintptr_t chunkPtr = Read(chunkPtrAddr);

// 步骤 4: 计算 Entity 指针的地址
uintptr_t entityPtrAddr = chunkPtr + 0x70 * indexInChunk;
//                                   │
//                                   └─ Stride

// 步骤 5: 读取 Entity 指针
uintptr_t entity = Read(entityPtrAddr);
```

---

## 最终正确公式

### 完整实现

```cpp
// 从 entity index 获取实体指针
uintptr_t GetEntityByIndex(uintptr_t entity_list, uint32_t index) {
    // 计算 Chunk 索引和 Chunk 内偏移
    uint32_t chunkIndex = index >> 9;      // 除以 512
    uint32_t indexInChunk = index & 0x1FF; // 对 512 取余

    // 读取 Chunk 指针
    uintptr_t listEntry = mem.read<uintptr_t>(
        entity_list + 0x10 + 0x8 * chunkIndex
    );

    if (!listEntry) return 0;

    // 读取 Entity 指针
    uintptr_t entity = mem.read<uintptr_t>(
        listEntry + 0x70 * indexInChunk
    );

    return entity;
}

// 从 handle 获取实体
uintptr_t GetEntityFromHandle(uintptr_t entity_list, uint32_t handle) {
    // 检查 Handle 有效性
    if (!handle || handle == 0xFFFFFFFF) return 0;

    // 提取 Index
    uint32_t index = handle & 0x7FFF;

    // 使用 Index 获取实体
    return GetEntityByIndex(entity_list, index);
}

// 从 Controller 获取 Pawn
uintptr_t GetPawnFromController(uintptr_t entity_list, uintptr_t controller) {
    // 读取 m_hPawn Handle
    uint32_t pawnHandle = mem.read<uint32_t>(controller + 0x62C);

    // 解析 Handle 获取 Pawn
    return GetEntityFromHandle(entity_list, pawnHandle);
}
```

### 使用示例

```cpp
void PrintPlayerInfo() {
    // 获取基址
    uintptr_t clientBase = GetModuleBase("client.dll");
    uintptr_t entityList = mem.read<uintptr_t>(clientBase + 0x19BBCC8);

    // 遍历所有玩家 Controller (索引 1-64)
    for (int i = 1; i < 64; i++) {
        // 获取 Controller
        uintptr_t controller = GetEntityByIndex(entityList, i);
        if (!controller) continue;

        // 获取 Pawn
        uintptr_t pawn = GetPawnFromController(entityList, controller);
        if (!pawn) continue;

        // 读取 Pawn 属性
        int health = mem.read<int>(pawn + 0x34C);
        int team = mem.read<int>(pawn + 0x3E3);
        vec3 pos = mem.read<vec3>(pawn + 0x1324);

        // 只显示存活的玩家
        if (health > 0 && health <= 100) {
            printf("Player %d: HP=%d, Team=%d, Pos=(%.1f, %.1f, %.1f)\n",
                   i, health, team, pos.x, pos.y, pos.z);
        }
    }
}
```

---

## 验证方法

### 方法 1: 血量验证

最简单的验证方法是检查血量：

```cpp
void VerifyWithHealth() {
    uintptr_t entityList = mem.read<uintptr_t>(clientBase + dwEntityList);

    for (int i = 1; i < 64; i++) {
        uintptr_t controller = GetEntityByIndex(entityList, i);
        if (!controller) continue;

        uintptr_t pawn = GetPawnFromController(entityList, controller);
        if (!pawn) continue;

        int health = mem.read<int>(pawn + 0x34C);

        // 血量应该在合理范围内
        if (health > 0 && health <= 100) {
            printf("✓ Player %d: Health = %d (Valid)\n", i, health);
        } else if (health != 0) {
            printf("✗ Player %d: Health = %d (Invalid!)\n", i, health);
        }
    }
}
```

### 方法 2: 坐标验证

检查坐标是否在地图范围内：

```cpp
void VerifyWithPosition() {
    uintptr_t entityList = mem.read<uintptr_t>(clientBase + dwEntityList);

    for (int i = 1; i < 64; i++) {
        uintptr_t pawn = GetPawnFromController(
            entityList,
            GetEntityByIndex(entityList, i)
        );
        if (!pawn) continue;

        vec3 pos = mem.read<vec3>(pawn + 0x1324);

        // CS2 地图坐标通常在 -10000 到 10000 之间
        if (abs(pos.x) < 10000 && abs(pos.y) < 10000 && abs(pos.z) < 10000) {
            printf("✓ Player %d: Pos=(%.1f, %.1f, %.1f) (Valid)\n",
                   i, pos.x, pos.y, pos.z);
        } else {
            printf("✗ Player %d: Pos=(%.1f, %.1f, %.1f) (Invalid!)\n",
                   i, pos.x, pos.y, pos.z);
        }
    }
}
```

### 方法 3: 队伍验证

队伍编号应该是 2 (T) 或 3 (CT)：

```cpp
void VerifyWithTeam() {
    uintptr_t entityList = mem.read<uintptr_t>(clientBase + dwEntityList);

    for (int i = 1; i < 64; i++) {
        uintptr_t pawn = GetPawnFromController(
            entityList,
            GetEntityByIndex(entityList, i)
        );
        if (!pawn) continue;

        int team = mem.read<int>(pawn + 0x3E3);

        if (team == 2 || team == 3) {
            printf("✓ Player %d: Team = %d (%s)\n",
                   i, team, team == 2 ? "T" : "CT");
        } else {
            printf("✗ Player %d: Team = %d (Invalid!)\n", i, team);
        }
    }
}
```

### 方法 4: 交叉验证

同时检查多个属性：

```cpp
bool IsValidPawn(uintptr_t pawn) {
    if (!pawn) return false;

    int health = mem.read<int>(pawn + 0x34C);
    int team = mem.read<int>(pawn + 0x3E3);
    vec3 pos = mem.read<vec3>(pawn + 0x1324);

    // 检查所有条件
    bool validHealth = (health >= 0 && health <= 100);
    bool validTeam = (team == 2 || team == 3);
    bool validPos = (abs(pos.x) < 10000 && abs(pos.y) < 10000);

    return validHealth && validTeam && validPos;
}
```

---

## 常见陷阱

### 陷阱 1: 混淆 Handle 和指针

❌ **错误**:
```cpp
uint32_t handle = mem.read<uint32_t>(controller + 0x62C);
uintptr_t pawn = (uintptr_t)handle;  // 错误！Handle 不是地址！
```

✓ **正确**:
```cpp
uint32_t handle = mem.read<uint32_t>(controller + 0x62C);
uintptr_t pawn = GetEntityFromHandle(entityList, handle);  // 需要解析
```

### 陷阱 2: 使用错误的 Stride

❌ **错误**:
```cpp
// 使用旧的错误 stride
uintptr_t entity = mem.read<uintptr_t>(chunkPtr + 0x78 * indexInChunk);
```

✓ **正确**:
```cpp
// 使用正确的 stride
uintptr_t entity = mem.read<uintptr_t>(chunkPtr + 0x70 * indexInChunk);
```

**影响**:
```
错误 stride 导致的偏移误差:
Index 10: 错误偏移 = 0x780, 正确偏移 = 0x700, 误差 = 0x80 (128 字节)
Index 20: 错误偏移 = 0xF00, 正确偏移 = 0xE00, 误差 = 0x100 (256 字节)
Index 30: 错误偏移 = 0x1680, 正确偏移 = 0x1500, 误差 = 0x180 (384 字节)

误差越来越大，读取的全是垃圾数据！
```

### 陷阱 3: 忘记检查空指针

❌ **错误**:
```cpp
uintptr_t controller = GetEntityByIndex(entityList, i);
uint32_t handle = mem.read<uint32_t>(controller + 0x62C);  // 可能崩溃！
```

✓ **正确**:
```cpp
uintptr_t controller = GetEntityByIndex(entityList, i);
if (!controller) continue;  // 检查空指针

uint32_t handle = mem.read<uint32_t>(controller + 0x62C);
if (!handle || handle == 0xFFFFFFFF) continue;  // 检查无效 Handle
```

### 陷阱 4: 使用错误的 Base Offset

❌ **错误**:
```cpp
// 使用 0x08 而不是 0x10
uintptr_t chunkPtr = mem.read<uintptr_t>(entityList + 0x08 + 0x8 * chunkIndex);
```

✓ **正确**:
```cpp
uintptr_t chunkPtr = mem.read<uintptr_t>(entityList + 0x10 + 0x8 * chunkIndex);
```

### 陷阱 5: 整数溢出

❌ **错误**:
```cpp
uint32_t index = handle & 0x7FFF;
uint32_t offset = 0x70 * index;  // 可能溢出！
uintptr_t addr = chunkPtr + offset;
```

✓ **正确**:
```cpp
uint32_t index = handle & 0x7FFF;
uintptr_t offset = (uintptr_t)0x70 * index;  // 使用 64 位计算
uintptr_t addr = chunkPtr + offset;
```

### 陷阱 6: 小端序问题

在 Cheat Engine 中查看内存时要注意字节序：

```
内存中的字节 (小端序):
00 80 04 8C FB 36 00 00

读取为 64 位整数:
0x36FB8C048000

不要搞反了！
```

### 陷阱 7: 偏移过时

游戏更新后偏移会改变：

```cpp
// 定期检查偏移是否仍然有效
bool VerifyOffsets() {
    uintptr_t localPawn = mem.read<uintptr_t>(clientBase + dwLocalPlayerPawn);
    if (!localPawn) return false;

    int health = mem.read<int>(localPawn + m_iHealth);

    // 血量应该在合理范围内
    if (health < 0 || health > 100) {
        printf("警告: 偏移可能已过时！\n");
        return false;
    }

    return true;
}
```

---

## 进阶话题

### 动态偏移查找

如果游戏更新导致偏移改变，可以使用模式扫描：

```cpp
// 查找特征码
uintptr_t PatternScan(const char* pattern, const char* mask) {
    uintptr_t moduleBase = GetModuleBase("client.dll");
    uintptr_t moduleSize = GetModuleSize("client.dll");

    for (uintptr_t i = 0; i < moduleSize - strlen(mask); i++) {
        bool found = true;
        for (size_t j = 0; j < strlen(mask); j++) {
            if (mask[j] != '?' &&
                pattern[j] != *(char*)(moduleBase + i + j)) {
                found = false;
                break;
            }
        }
        if (found) return moduleBase + i;
    }
    return 0;
}
```

### 优化性能

缓存 Chunk 指针以减少内存读取：

```cpp
class EntityListCache {
private:
    uintptr_t chunkPtrs[64];  // 缓存 Chunk 指针
    uint64_t lastUpdate;

public:
    void Update(uintptr_t entityList) {
        // 每秒更新一次缓存
        if (GetTickCount64() - lastUpdate < 1000) return;

        for (int i = 0; i < 64; i++) {
            chunkPtrs[i] = mem.read<uintptr_t>(
                entityList + 0x10 + 0x8 * i
            );
        }

        lastUpdate = GetTickCount64();
    }

    uintptr_t GetEntity(uint32_t index) {
        uint32_t chunkIndex = index >> 9;
        uint32_t indexInChunk = index & 0x1FF;

        if (chunkIndex >= 64 || !chunkPtrs[chunkIndex]) return 0;

        return mem.read<uintptr_t>(
            chunkPtrs[chunkIndex] + 0x70 * indexInChunk
        );
    }
};
```

### 多线程安全

如果在多线程环境中使用：

```cpp
#include <mutex>

class ThreadSafeEntityList {
private:
    std::mutex mtx;
    uintptr_t entityList;

public:
    uintptr_t GetEntity(uint32_t index) {
        std::lock_guard<std::mutex> lock(mtx);
        return GetEntityByIndex(entityList, index);
    }
};
```

---

## Entity List 内存结构图解（旧版，保留参考）

```
┌─────────────────────────────────────────────────────────────────┐
│                    Entity List (dwEntityList)                    │
├─────────────────────────────────────────────────────────────────┤
│ +0x00: [保留/未知]                                               │
│ +0x08: [保留/未知]                                               │
│ +0x10: chunk[0] 指针 ──────┐                                    │
│ +0x18: chunk[1] 指针 ──────┼──┐                                 │
│ +0x20: chunk[2] 指针 ──────┼──┼──┐                              │
│ +0x28: chunk[3] 指针 ──────┼──┼──┼──┐                           │
│ ...                        │  │  │  │                           │
└────────────────────────────┼──┼──┼──┼───────────────────────────┘
                             │  │  │  │
                             ▼  │  │  │
┌────────────────────────────┐  │  │  │
│      Chunk[0] 数组         │  │  │  │
│  (Entity 0 ~ 511)          │  │  │  │
├────────────────────────────┤  │  │  │
│ +0x000: Entity[0] 指针     │  │  │  │
│ +0x070: Entity[1] 指针     │  │  │  │
│ +0x0E0: Entity[2] 指针     │  │  │  │
│ +0x150: Entity[3] 指针     │  │  │  │
│ ...                        │  │  │  │
│ +stride * 511: Entity[511] │  │  │  │
└────────────────────────────┘  │  │  │
                                ▼  │  │
               ┌────────────────┐  │  │
               │   Chunk[1]     │  │  │
               │ (Entity 512~   │  │  │
               │  1023)         │  │  │
               └────────────────┘  │  │
                                   ▼  │
                  ┌────────────────┐  │
                  │   Chunk[2]     │◄─┘  ← LocalPlayerPawn 在这里！
                  │ (Entity 1024~  │      (index=1052, chunk=2, offset=28)
                  │  1535)         │
                  └────────────────┘
```

### 关键参数

| 参数 | 值 | 说明 |
|------|-----|------|
| Base Offset | `0x10` | chunk 数组起始位置 |
| Chunk Pointer Size | `0x08` | 每个 chunk 指针占 8 字节 |
| Stride | `0x70` | 每个 entity 条目占 112 字节 |
| Entities per Chunk | `512` | 每个 chunk 包含 512 个实体 |

## ESP 遍历代码详解

```cpp
void esp::updateEntities()
{
    // 1. 获取 Entity List 基址
    uintptr_t entity_list = Read(modBase + dwEntityList);

    // 2. 遍历 Controller (玩家索引 1-63)
    for (uint32_t i = 1; i < 64; i++)
    {
        // 3. 计算 Controller 所在的 chunk 和偏移
        //    对于小索引 (1-63)，chunkIndex 始终为 0
        uintptr_t listEntry = Read(entity_list + 0x10 + 0x8 * (i >> 9));
        if (!listEntry) continue;

        // 4. 读取 Controller 指针
        uintptr_t entityController = Read(listEntry + 0x70 * (i & 0x1FF));
        if (!entityController) continue;

        // 5. 从 Controller 获取 Pawn Handle
        uint32_t pawnHandle = Read<uint32_t>(entityController + m_hPawn);
        if (!pawnHandle) continue;

        // 6. 解析 Handle，定位 Pawn
        uint32_t pawnIndex = pawnHandle & 0x7FFF;
        uint32_t pawnChunk = pawnIndex >> 9;        // 除以 512
        uint32_t pawnInChunk = pawnIndex & 0x1FF;   // 对 512 取余

        // 7. 读取 Pawn 所在 chunk
        uintptr_t pawnListEntry = Read(entity_list + 0x10 + 0x8 * pawnChunk);

        // 8. 读取 Pawn 指针
        uintptr_t entity = Read(pawnListEntry + 0x70 * pawnInChunk);

        // 9. 现在可以读取 Pawn 的属性了
        int32_t health = Read<int32_t>(entity + m_iHealth);
        vec3 position = Read<vec3>(entity + m_vOldOrigin);
        // ...
    }
}
```

---

## 快速参考卡片

### 一页纸速查表

```
╔═══════════════════════════════════════════════════════════════════════╗
║                CS2 Entity List 逆向工程速查表                         ║
╠═══════════════════════════════════════════════════════════════════════╣
║                                                                       ║
║  【关键常量】                                                         ║
║  ├─ Base Offset:    0x10                                             ║
║  ├─ Stride:         0x70 (112 字节)                                  ║
║  ├─ Chunk Size:     512 (0x200)                                      ║
║  ├─ Chunk 指针大小: 8 字节                                            ║
║  └─ Handle 掩码:    0x7FFF (低 15 位)                                ║
║                                                                       ║
║  【重要偏移】                                                         ║
║  ├─ dwEntityList:   0x19BBCC8 (client.dll)                           ║
║  ├─ m_hPawn:        0x62C (Controller)                               ║
║  ├─ m_iHealth:      0x34C (Pawn)                                     ║
║  ├─ m_iTeamNum:     0x3E3 (Pawn)                                     ║
║  ├─ m_vOldOrigin:   0x1324 (Pawn)                                    ║
║  └─ m_ArmorValue:   0x1224 (Pawn)                                    ║
║                                                                       ║
║  【核心公式】                                                         ║
║                                                                       ║
║  1️⃣ 获取 Controller:                                                 ║
║     chunkIndex = playerIndex >> 9                                    ║
║     indexInChunk = playerIndex & 0x1FF                               ║
║     chunk = Read(EntityList + 0x10 + 0x8 * chunkIndex)               ║
║     controller = Read(chunk + 0x70 * indexInChunk)                   ║
║                                                                       ║
║  2️⃣ 解析 Handle:                                                     ║
║     index = handle & 0x7FFF                                          ║
║     chunkIndex = index >> 9                                          ║
║     indexInChunk = index & 0x1FF                                     ║
║                                                                       ║
║  3️⃣ 获取 Pawn:                                                       ║
║     chunk = Read(EntityList + 0x10 + 0x8 * chunkIndex)               ║
║     pawn = Read(chunk + 0x70 * indexInChunk)                         ║
║                                                                       ║
║  【验证检查】                                                         ║
║  ✓ 血量: 0 ≤ health ≤ 100                                            ║
║  ✓ 队伍: team == 2 (T) 或 team == 3 (CT)                             ║
║  ✓ 坐标: |x|, |y|, |z| < 10000                                       ║
║  ✓ 指针: 非空且对齐到 8 字节                                          ║
║                                                                       ║
║  【常见错误】                                                         ║
║  ✗ 使用 Base Offset = 0x00 或 0x08                                   ║
║  ✗ 使用 Stride = 0x78                                                ║
║  ✗ 直接使用 Handle 作为指针                                           ║
║  ✗ 忘记检查指针是否为空                                               ║
║  ✗ 使用过时的偏移量                                                   ║
║                                                                       ║
║  【真实数据示例】                                                     ║
║  Entity List:     0x332089C0000                                      ║
║  Chunk[0]:        0x33208CF0008                                      ║
║  Controller:      0x332CFD50000                                      ║
║  Handle:          0x005E80C0 → Index 192                             ║
║  Pawn:            0x332CFD98000                                      ║
║  Health:          100                                                ║
║                                                                       ║
║  【性能优化】                                                         ║
║  • 缓存 Chunk 指针（减少 1 次读取）                                   ║
║  • 批量读取多个属性（减少函数调用）                                   ║
║  • 使用 ReadProcessMemory 一次读取整个结构                            ║
║                                                                       ║
╚═══════════════════════════════════════════════════════════════════════╝
```

### 调试检查清单

当你的代码不工作时，按顺序检查：

- [ ] **1. Entity List 基址是否正确？**
  ```cpp
  printf("Entity List: 0x%llx\n", entityList);
  // 应该是一个大的地址，例如 0x332089C0000
  ```

- [ ] **2. Chunk 指针是否有效？**
  ```cpp
  uintptr_t chunk = Read<uintptr_t>(entityList + 0x10);
  printf("Chunk[0]: 0x%llx\n", chunk);
  // 应该非空且看起来像一个有效地址
  ```

- [ ] **3. Controller 指针是否有效？**
  ```cpp
  uintptr_t controller = Read<uintptr_t>(chunk + 0x70 * playerIndex);
  printf("Controller: 0x%llx\n", controller);
  // 应该非空
  ```

- [ ] **4. Handle 是否非零？**
  ```cpp
  uint32_t handle = Read<uint32_t>(controller + 0x62C);
  printf("Handle: 0x%08x\n", handle);
  // 如果是 0，玩家可能已死亡或不存在
  ```

- [ ] **5. Handle 解析是否正确？**
  ```cpp
  uint32_t index = handle & 0x7FFF;
  printf("Index: %u\n", index);
  // 应该是一个合理的数字（通常 < 2048）
  ```

- [ ] **6. Pawn 指针是否有效？**
  ```cpp
  uintptr_t pawn = Read<uintptr_t>(chunk + 0x70 * indexInChunk);
  printf("Pawn: 0x%llx\n", pawn);
  // 应该非空
  ```

- [ ] **7. 血量是否合理？**
  ```cpp
  int health = Read<int>(pawn + 0x34C);
  printf("Health: %d\n", health);
  // 应该在 0-100 之间
  ```

- [ ] **8. 队伍是否合理？**
  ```cpp
  int team = Read<int>(pawn + 0x3E3);
  printf("Team: %d\n", team);
  // 应该是 2 或 3
  ```

### Cheat Engine 快速操作

| 操作 | 快捷键 | 说明 |
|------|--------|------|
| 跳转到地址 | `Ctrl+G` | 输入十六进制地址 |
| 添加地址到列表 | `Ctrl+A` | 手动添加地址 |
| 扫描未知初始值 | `首次扫描` | 开始新扫描 |
| 扫描变化的值 | `再次扫描` | 过滤结果 |
| 查找指针 | `右键 → 指针扫描` | 查找指向地址的指针 |
| 查看内存区域 | `Ctrl+M` | 打开内存查看器 |
| 十六进制视图 | `内存查看器` | 查看原始字节 |
| 计算器 | `Ctrl+Alt+C` | 十六进制计算器 |

### 常用代码片段

#### 安全的内存读取
```cpp
template<typename T>
bool SafeRead(uintptr_t address, T& out) {
    if (!address) return false;
    SIZE_T bytesRead;
    if (!ReadProcessMemory(hProcess, (LPCVOID)address, &out, sizeof(T), &bytesRead)) {
        return false;
    }
    return bytesRead == sizeof(T);
}
```

#### 验证指针
```cpp
bool IsValidPointer(uintptr_t ptr) {
    if (!ptr) return false;
    if (ptr < 0x10000) return false;  // 太小
    if (ptr % 8 != 0) return false;   // 未对齐

    // 尝试读取第一个字节
    uint8_t test;
    return SafeRead(ptr, test);
}
```

#### 批量读取结构
```cpp
struct PlayerData {
    int health;
    int team;
    float position[3];
};

bool ReadPlayerData(uintptr_t pawn, PlayerData& data) {
    if (!SafeRead(pawn + 0x34C, data.health)) return false;
    if (!SafeRead(pawn + 0x3E3, data.team)) return false;
    if (!SafeRead(pawn + 0x1324, data.position)) return false;
    return true;
}
```

---

## 快速参考

### 关键常量

```cpp
// 偏移量 (需要定期从 cs2-dumper 更新)
constexpr uintptr_t dwEntityList = 0x19BBCC8;
constexpr uintptr_t dwLocalPlayerPawn = 0x1825128;

// Entity List 结构参数
constexpr uintptr_t BASE_OFFSET = 0x10;    // Chunk 数组起始偏移
constexpr uintptr_t CHUNK_SIZE = 0x08;     // 每个 Chunk 指针大小
constexpr uintptr_t STRIDE = 0x70;         // Entity 条目间距
constexpr uint32_t ENTITIES_PER_CHUNK = 512;

// Pawn 偏移量
constexpr uintptr_t m_hPawn = 0x62C;       // Controller -> Pawn Handle
constexpr uintptr_t m_iHealth = 0x34C;     // 血量
constexpr uintptr_t m_iTeamNum = 0x3E3;    // 队伍
constexpr uintptr_t m_vOldOrigin = 0x1324; // 坐标
constexpr uintptr_t m_vecViewOffset = 0xCB0; // 视角偏移
```

### 核心函数

```cpp
// 从 Index 获取实体
inline uintptr_t GetEntityByIndex(uintptr_t entityList, uint32_t index) {
    uint32_t chunkIndex = index >> 9;
    uint32_t indexInChunk = index & 0x1FF;

    uintptr_t chunk = mem.read<uintptr_t>(
        entityList + BASE_OFFSET + CHUNK_SIZE * chunkIndex
    );
    if (!chunk) return 0;

    return mem.read<uintptr_t>(chunk + STRIDE * indexInChunk);
}

// 从 Handle 获取实体
inline uintptr_t GetEntityFromHandle(uintptr_t entityList, uint32_t handle) {
    if (!handle || handle == 0xFFFFFFFF) return 0;
    return GetEntityByIndex(entityList, handle & 0x7FFF);
}

// 从 Controller 获取 Pawn
inline uintptr_t GetPawnFromController(uintptr_t entityList, uintptr_t controller) {
    if (!controller) return 0;
    uint32_t handle = mem.read<uint32_t>(controller + m_hPawn);
    return GetEntityFromHandle(entityList, handle);
}
```

### 常用代码片段

#### 遍历所有玩家

```cpp
void IterateAllPlayers() {
    uintptr_t entityList = mem.read<uintptr_t>(clientBase + dwEntityList);

    for (int i = 1; i < 64; i++) {
        uintptr_t controller = GetEntityByIndex(entityList, i);
        if (!controller) continue;

        uintptr_t pawn = GetPawnFromController(entityList, controller);
        if (!pawn) continue;

        // 处理玩家...
    }
}
```

#### 获取本地玩家

```cpp
uintptr_t GetLocalPlayer() {
    return mem.read<uintptr_t>(clientBase + dwLocalPlayerPawn);
}
```

#### 检查是否是敌人

```cpp
bool IsEnemy(uintptr_t localPawn, uintptr_t targetPawn) {
    int localTeam = mem.read<int>(localPawn + m_iTeamNum);
    int targetTeam = mem.read<int>(targetPawn + m_iTeamNum);
    return localTeam != targetTeam;
}
```

---

## 原错误分析

### 问题背景

从 a2x-dumper 获取的偏移无法正确读取实体数据。主要问题：
- `dwEntityList` 偏移正确，但内部结构解析错误
- 原代码使用 `stride = 0x78`，实际应该是 `0x70`
- 原代码使用 `base offset = 0x08`，实际应该是 `0x10`

### 错误对比

| 参数 | 错误值 | 正确值 | 影响 |
|------|--------|--------|------|
| Base Offset | `0x08` | `0x10` | 指向错误的 Chunk 数组，读取错误的 Chunk 指针 |
| Stride | `0x78` | `0x70` | 计算 Entity 偏移错误，误差随 Index 增大 |

### 错误公式的影响

```
使用错误参数读取 Entity[28]:

错误公式:
  chunkPtr = Read(entityList + 0x08 + 0x8 * 2)  // 错误的 Chunk
  entity = Read(chunkPtr + 0x78 * 28)           // 错误的偏移 = 0x10E0
  结果: 读取到垃圾数据

正确公式:
  chunkPtr = Read(entityList + 0x10 + 0x8 * 2)  // 正确的 Chunk
  entity = Read(chunkPtr + 0x70 * 28)           // 正确的偏移 = 0xC40
  结果: 正确读取 LocalPlayerPawn
```

### Stride 误差累积

```
Index | 错误偏移 (0x78) | 正确偏移 (0x70) | 误差
------|----------------|----------------|------
  1   |     0x78       |     0x70       |  0x08
  10  |    0x780       |    0x700       |  0x80
  28  |    0x10E0      |    0xC40       | 0x4A0 (1184 字节!)
  50  |    0x1900      |    0x1590      | 0x370
 100  |    0x3200      |    0x2C00      | 0x600

误差越来越大，完全读不到正确数据！
```

---

## 总结

### 关键发现

| 发现 | 说明 | 如何验证 |
|------|------|----------|
| Entity List Chunk 数组从 `+0x10` 开始 | 不是 `+0x08` | 在 CE 中查看 Entity List 内存布局 |
| 每个实体条目间距是 `0x70` 字节 | 不是 `0x78` | 计算 `offset / indexInChunk` |
| Handle 的低 15 位是实体索引 | `index = handle & 0x7FFF` | 用计算器验证位运算 |
| 每个 Chunk 包含 512 个实体 | `chunkIndex = index >> 9` | 512 = 2^9 |
| Chunk 指针间隔 8 字节 | 64 位指针大小 | 观察内存中的指针数组 |

### 逆向工程方法论

这个方法可以应用于任何类似的逆向工程任务：

```
┌─────────────────────────────────────────────────────────────┐
│  第一步: 找到"锚点"                                          │
│  ├─ 找到一个你确定正确的值                                   │
│  ├─ 例如: LocalPlayerPawn 地址                              │
│  └─ 通过已知偏移直接读取                                     │
└────────────────────────┬────────────────────────────────────┘
                         ▼
┌─────────────────────────────────────────────────────────────┐
│  第二步: 理解数据结构                                        │
│  ├─ 研究 Handle/Index 系统                                  │
│  ├─ 理解分块 (Chunking) 机制                                │
│  └─ 计算理论上的位置                                         │
└────────────────────────┬────────────────────────────────────┘
                         ▼
┌─────────────────────────────────────────────────────────────┐
│  第三步: 暴力搜索                                            │
│  ├─ 在可能的内存区域搜索锚点值                               │
│  ├─ 记录所有找到的位置                                       │
│  └─ 分析位置之间的关系                                       │
└────────────────────────┬────────────────────────────────────┘
                         ▼
┌─────────────────────────────────────────────────────────────┐
│  第四步: 反推参数                                            │
│  ├─ 从找到的位置计算 Stride                                 │
│  ├─ 测试不同的 Base Offset                                  │
│  └─ 验证公式的正确性                                         │
└────────────────────────┬────────────────────────────────────┘
                         ▼
┌─────────────────────────────────────────────────────────────┐
│  第五步: 多点验证                                            │
│  ├─ 用多个不同的实体测试                                     │
│  ├─ 检查边界情况                                             │
│  └─ 确保公式普遍适用                                         │
└─────────────────────────────────────────────────────────────┘
```

### 学到的技能

完成本教程后，你应该掌握：

✅ **内存分析技能**
- 使用 Cheat Engine 浏览和搜索内存
- 理解指针和多级指针
- 识别内存中的数据结构

✅ **逆向工程思维**
- 从已知推导未知
- 使用暴力搜索找到模式
- 通过多点验证确认假设

✅ **编程技能**
- 位运算的实际应用
- 内存读取和指针操作
- 数据结构的实现

✅ **调试技能**
- 系统化的问题定位
- 交叉验证方法
- 错误排查流程

---

## 视频教程大纲

如果你要制作视频教程，建议按以下结构：

### 第一部分: 理论基础 (10-15 分钟)

1. **什么是 Entity List？** (2 分钟)
   - 游戏实体的概念
   - 为什么需要 Entity List
   - ESP 的工作原理

2. **CS2 的双实体系统** (3 分钟)
   - Controller vs Pawn
   - Handle 机制
   - 为什么要分开

3. **内存结构概览** (5 分钟)
   - Chunk 系统
   - Stride 概念
   - 完整的内存布局图

4. **前置知识检查** (2 分钟)
   - 需要掌握的技能
   - 工具准备清单

### 第二部分: 工具使用 (15-20 分钟)

5. **Cheat Engine 基础** (5 分钟)
   - 安装和配置
   - 附加进程
   - 基本操作演示

6. **找到 client.dll 基址** (3 分钟)
   - Memory Regions 窗口
   - 定位模块基址
   - 使用 Go to Address

7. **获取初始偏移** (5 分钟)
   - 使用 a2x cs2-dumper
   - 理解偏移文件
   - 导入到项目

8. **验证基础偏移** (5 分钟)
   - 读取 LocalPlayerPawn
   - 验证血量
   - 确认锚点正确

### 第三部分: 实战逆向 (25-30 分钟)

9. **第一步: 获取锚点** (5 分钟)
   - 使用 dwLocalPlayerPawn
   - 在 CE 中验证
   - 记录地址

10. **第二步: 理解 Handle** (5 分钟)
    - 读取 m_hPawn
    - 手动解析 Handle
    - 计算 Index 和 Chunk

11. **第三步: 搜索 Pawn 地址** (8 分钟)
    - 在 Entity List 中搜索
    - 找到 Chunk 指针
    - 计算偏移

12. **第四步: 计算 Stride** (5 分钟)
    - 使用公式计算
    - 在 CE 中验证
    - 对比错误值

13. **第五步: 确定 Base Offset** (5 分钟)
    - 测试不同值
    - 找到正确的 0x10
    - 解释为什么

### 第四部分: 编程实现 (20-25 分钟)

14. **编写核心函数** (10 分钟)
    - GetEntityByIndex
    - GetEntityFromHandle
    - GetPawnFromController

15. **遍历所有玩家** (5 分钟)
    - 循环 Controller
    - 读取 Pawn 属性
    - 显示结果

16. **验证和调试** (5 分钟)
    - 多实体测试
    - 边界情况
    - 错误处理

17. **常见错误演示** (5 分钟)
    - 使用错误 Stride 的后果
    - 忘记检查空指针
    - 如何排查问题

### 第五部分: 总结和扩展 (10 分钟)

18. **知识回顾** (3 分钟)
    - 关键参数总结
    - 公式速查

19. **实际应用** (4 分钟)
    - ESP 实现
    - 性能优化
    - 反作弊对抗

20. **进阶方向** (3 分钟)
    - 动态偏移查找
    - 模式扫描
    - 其他游戏的应用

**总时长**: 约 80-100 分钟

**建议分集**:
- 第一集: 理论基础 + 工具使用 (25-35 分钟)
- 第二集: 实战逆向 (25-30 分钟)
- 第三集: 编程实现 + 总结 (30-35 分钟)

---

## 练习题

### 初级练习

1. **计算练习**
   - Handle = 0x02A5061F，计算 Index、ChunkIndex、IndexInChunk
   - Index = 1500，计算应该在哪个 Chunk 的哪个位置

2. **内存读取**
   - 在 CE 中找到 Entity List 基址
   - 读取前 4 个 Chunk 指针
   - 验证它们是否有效

3. **验证偏移**
   - 读取 LocalPlayerPawn 的血量
   - 修改血量，观察游戏中的变化
   - 验证 m_iTeamNum 偏移

### 中级练习

4. **实现函数**
   - 编写 GetEntityByIndex 函数
   - 添加错误检查
   - 测试边界情况

5. **遍历实体**
   - 遍历所有 Controller
   - 打印每个玩家的信息
   - 统计 T 和 CT 的数量

6. **Handle 解析**
   - 从多个 Controller 读取 m_hPawn
   - 解析每个 Handle
   - 验证对应的 Pawn 是否正确

### 高级练习

7. **性能优化**
   - 实现 Chunk 指针缓存
   - 测量优化前后的性能差异
   - 分析内存读取次数

8. **错误诊断**
   - 故意使用错误的 Stride (0x78)
   - 观察读取到的数据
   - 解释为什么会出错

9. **动态查找**
   - 实现自动查找 Stride 的算法
   - 不依赖已知的 0x70 值
   - 验证算法的准确性

---

## 常见问题 (FAQ)

### Q1: 游戏更新后偏移失效怎么办？

**A**:
1. 访问 [a2x cs2-dumper](https://github.com/a2x/cs2-dumper) 获取最新偏移
2. 更新你的代码中的偏移常量
3. 重新验证 Entity List 结构参数 (Base Offset, Stride)
4. 如果结构改变，重新执行本教程的逆向过程

### Q2: 为什么我读取的血量是负数或很大的数？

**A**: 可能的原因：
- 偏移量错误或过时
- Stride 计算错误
- 读取了错误的内存地址
- Pawn 指针无效

解决方法：
```cpp
// 添加验证
int health = mem.read<int>(pawn + m_iHealth);
if (health < 0 || health > 100) {
    printf("警告: 血量异常 (%d)，可能偏移错误\n", health);
}
```

### Q3: Controller 和 Pawn 的区别是什么？

**A**:
- **Controller**: 玩家的"逻辑"实体，包含玩家名、队伍、金钱等
- **Pawn**: 玩家的"物理"实体，包含位置、血量、模型等
- 玩家死亡时 Pawn 被销毁，但 Controller 保留
- 必须通过 Controller 的 m_hPawn Handle 找到对应的 Pawn

### Q4: 为什么 Chunk 大小是 512？

**A**:
- 512 = 2^9，方便用位运算快速计算
- `chunkIndex = index >> 9` 等价于 `index / 512`
- `indexInChunk = index & 0x1FF` 等价于 `index % 512`
- 这是游戏引擎的设计选择，平衡了内存和性能

### Q5: 如何处理多线程安全？

**A**:
```cpp
#include <mutex>

class SafeEntityList {
    std::mutex mtx;

public:
    uintptr_t GetEntity(uint32_t index) {
        std::lock_guard<std::mutex> lock(mtx);
        // 读取操作
    }
};
```

### Q6: 读取内存会被反作弊检测吗？

**A**:
- 外部读取（ReadProcessMemory）相对安全
- 但仍可能被检测到
- 建议：
  - 不要在竞技模式使用
  - 只在本地服务器测试
  - 了解风险和后果

### Q7: 如何找到其他实体（武器、C4 等）？

**A**:
- 武器和道具也在 Entity List 中
- 索引通常在 64 以后
- 需要检查实体类型来区分
- 可以通过 VTable 或类名识别

### Q8: Stride 会随游戏更新改变吗？

**A**:
- 可能会，但不常见
- 通常只有大版本更新才会改变
- 建议每次游戏更新后重新验证
- 可以实现自动检测机制

---

## 参考资源

### 官方资源

- [CS2 官方网站](https://www.counter-strike.net/)
- [Source 2 引擎文档](https://developer.valvesoftware.com/wiki/Source_2)

### 社区资源

- [a2x cs2-dumper](https://github.com/a2x/cs2-dumper) - 自动偏移提取工具
- [UnknownCheats CS2 板块](https://www.unknowncheats.me/forum/counter-strike-2-a/) - 逆向工程讨论
- [MPGH CS2 板块](https://www.mpgh.net/forum/forumdisplay.php?f=1056) - 作弊开发社区

### 学习资源

- [Cheat Engine 教程](https://wiki.cheatengine.org/index.php?title=Tutorials)
- [逆向工程入门](https://github.com/mytechnotalent/Reverse-Engineering)
- [Windows 内存管理](https://docs.microsoft.com/en-us/windows/win32/memory/memory-management)

### 工具下载

- [Cheat Engine](https://www.cheatengine.org/)
- [x64dbg](https://x64dbg.com/)
- [IDA Pro / Ghidra](https://ghidra-sre.org/)
- [HxD Hex Editor](https://mh-nexus.de/en/hxd/)

---

## 致谢

- **a2x** - cs2-dumper 工具的作者
- **UnknownCheats 社区** - 逆向工程知识分享
- **Valve** - 创造了 CS2 和 Source 2 引擎
- **所有贡献者** - 帮助完善本文档

---

## 许可证

本文档仅供教育和学习目的使用。

**免责声明**:
- 本文档内容仅用于学习逆向工程技术
- 不鼓励在在线游戏中使用作弊工具
- 使用本文档中的技术可能违反游戏服务条款
- 作者不对任何滥用行为负责

---

## 更新日志

### 2024-01-22
- ✅ 完整重写文档，添加详细教程
- ✅ 添加前置知识章节
- ✅ 添加分步教程和 Cheat Engine 详细步骤
- ✅ 添加内存结构图解
- ✅ 添加验证方法和常见陷阱
- ✅ 添加视频教程大纲
- ✅ 添加练习题和 FAQ
- ✅ 添加参考资源

### 历史版本
- 初始版本 - 基础逆向过程记录

---

**文档结束**

如有问题或建议，欢迎提交 Issue 或 Pull Request！
